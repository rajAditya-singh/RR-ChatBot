import { CreateChatCompletionRequest, CreateChatCompletionResponse } from 'openai';
import { FreeGPTModel } from './FreeGPTModel';
export declare class OpenAIFree implements FreeGPTModel {
    private axios;
    constructor(openAiUrl: string, accessToken: string);
    createChatCompletion(request: CreateChatCompletionRequest, customVariable?: {
        conversationId?: string;
        parentMessageId?: string;
    }): Promise<CreateChatCompletionResponse>;
    deleteConversation(conversationId: string): Promise<void>;
}
